package testfiles.DefnVals.WithoutDeclaredType.InClass.Inferrable.TermApply.WithTermName.Left;

import io.vavr.control.Either;
import io.vavr.control.Either.left;

public class Sample {
    private final Either<RuntimeException> x = left(new RuntimeException());
}