package testfiles.Fors.For.TwoEnumerators;

import java.util.List;
import java.util.List.of;
import testfilesext.SampleObject.func4;

public class Sample {

    public void foo() {
        final List<int> xs = of(1,
            2,
            3);
        final List<int> ys = of(11,
            22,
            33);
        xs.forEach(x -> ys.forEach(y -> func4(x, y)));
    }
}